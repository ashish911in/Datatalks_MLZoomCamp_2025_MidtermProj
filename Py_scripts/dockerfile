# Use the latest stable, minimal Python image
FROM python:3.14-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the Python script into the container
COPY trial.py /app/

# Define the command to run the script when the container starts.
# You must run the container interactively (docker run -it) 
# for the input() function to work.
CMD ["python", "trial.py"]